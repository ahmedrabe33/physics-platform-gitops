apiVersion: apps/v1
kind: Deployment

metadata:
  name: progress-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: progress-service

  template:
    metadata:
      labels:
        app: progress-service

    spec:
      containers:
        - name: progress-service
          image: physics-progress:v2
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 3004

          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: progress-config
                  key: PORT

            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: progress-config
                  key: NODE_ENV

            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: progress-config
                  key: DB_HOST

            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: progress-config
                  key: DB_PORT

            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: progress-config
                  key: POSTGRES_DB

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD

          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 20
            periodSeconds: 10
